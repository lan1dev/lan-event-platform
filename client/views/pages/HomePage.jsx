import React from 'react';
import HomePageBase from './HomePageBase';

/**
 * HomePage for the root (/) route
 */
const HomePage = () => <HomePageBase>Sections are missing...</HomePageBase>;

export default HomePage;
